
Routine Fibonacci(x)
    If(x < 2)
        Return x
    Else
        Return Fibonacci(x - 1) + Fibonacci(x - 2)
    EndIf
EndRoutine

Routine Main()
    Print "Fibonacci(28) : ", Fibonacci(28)
EndRoutine
