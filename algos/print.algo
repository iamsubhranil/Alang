Routine Main()
    Call Print("Hello World!")
    Set i = 2
    Call Print(i)
    If(i > 2)
        Call Print("i is greater than 2!")
    Else
        Call Print("i is equal to 2!")
    EndIf
EndRoutine
