Begin
Set i = N/2
While(i > 2)
Begin
    If(N % i == 0)
    Then
        Set temp = i
        Set flag = 0
        Set j = 2
        While(j < i/2)
        Begin
            If(i % j == 0)
            Then
                Set flag = 1
            Else 
                If(i % 2 == 0)
                Then
                    Set flag = 2
                EndIf
            EndIf
            Set j = j + 1
        EndWhile
        If(flag == 0)
        Then
            Print "temp"
        EndIf
    EndIf
    Set i = i - 1
EndWhile
End
